{
    "lea_id": {{lea_id}},
    "lea_name": "{{lea_name}}",
    "latitude": {% if not latitude %}NULL{% else %}{{latitude}}{% endif %},
    "longitude": {% if not longitude %}NULL{% else %}{{longitude}}{% endif %},
    "lowest_grade_offered": "{{lowest_grade_offered}}",
    "highest_grade_offered": "{{highest_grade_offered}}",
    "num_schools": {{num_schools}},
    "num_students_enrolled": {{num_students_enrolled}}
}